// Intentionally empty.
//
// This was a scratch debug script used once to print
// `claude_conformance::fixture_report()`'s scenario outcomes while
// building the `fixtures/conformance/fixture-mode-baseline.json` entry
// for "claude-tui". Not part of the suite -- left in place, empty, only
// because this session's sandbox permissions would not allow deleting
// the file outright. Whoever picks this up should run:
//
//   rm crates/runtime/tests/print_claude_tui_report.rs
